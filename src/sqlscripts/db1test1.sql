CREATE TABLE IF NOT EXISTS TableC (
	columnc1 INT AUTO_INCREMENT,
    columnc2 VARCHAR(30),
    columnc4 BOOLEAN,
    columnc7 INT,
    CONSTRAINT pk_tableC PRIMARY KEY(columnc1)
);

CREATE TABLE IF NOT EXISTS TableX (
	columnx INT PRIMARY KEY
);
